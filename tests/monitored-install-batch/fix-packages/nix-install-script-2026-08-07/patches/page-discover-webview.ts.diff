--- lib/page-discover-webview.ts	2026-07-31 01:52:03
+++ /Users/tariqwest/Developer/allbrew/tests/monitored-install-batch/worktrees/nix-2026-08-07/lib/page-discover-webview.ts	2026-08-07 07:50:53
@@ -229,6 +229,8 @@
         const seen = new Set();
         const add = (url, evidence) => {
           if (!url) return;
+          // Drop shell/template placeholders (e.g. nix-2.35.1-$system.tar.xz)
+          if (/\$[A-Za-z_{]/.test(url) || /\$\{[^}]+\}/.test(url)) return;
           try {
             assertSafePublicFetchUrl(url);
           } catch {
