--- lib/page-discover.ts	2026-08-07 07:32:50
+++ /Users/tariqwest/Developer/allbrew/tests/monitored-install-batch/worktrees/nix-2026-08-07/lib/page-discover.ts	2026-08-07 07:50:53
@@ -68,6 +68,11 @@
   if (!raw) return null;
   let href = raw.trim().replace(/^['"]|['"]$/g, "");
   if (!href || IGNORE_HREF_RE.test(href)) return null;
+  // Unexpanded shell/template placeholders (e.g. nix-2.35.1-$system.tar.xz)
+  // are not downloadable URLs — never promote them as candidates.
+  if (/\$[A-Za-z_{]/.test(href) || /\$\{[^}]+\}/.test(href)) {
+    return null;
+  }
   try {
     const abs = new URL(href, base).href;
     assertSafePublicFetchUrl(abs);
@@ -77,6 +82,10 @@
     if (/\.(png|jpe?g|gif|svg|webp|ico|css|js|map|woff2?)(?:\?|#|$)/i.test(u.pathname)) {
       return null;
     }
+    // Path may still contain $system after resolve
+    if (/\$[A-Za-z_{]/.test(u.pathname) || /\$\{[^}]+\}/.test(u.href)) {
+      return null;
+    }
     return abs;
   } catch {
     return null;
@@ -100,6 +109,16 @@
   pageUrl: string,
   evidence: string[] = [],
 ): DiscoverCandidate {
+  // Unexpanded shell placeholders are never installable
+  if (/\$[A-Za-z_{]/.test(url) || /\$\{[^}]+\}/.test(url)) {
+    return {
+      url,
+      kind: "unknown",
+      score: -1000,
+      evidence: [...evidence, "shell-placeholder"],
+      source: "static",
+    };
+  }
   const classified = classify(url);
   let score = 0;
   const ev = [...evidence];
@@ -503,7 +522,7 @@
         timeoutMs: 20_000,
       });
       const body = fetched.body;
-      // direct absolute artifact URLs in JS
+      // direct artifact URLs in JS
       const bare = body.match(/https?:\/\/[^"'\\s)]+\.(?:dmg|pkg|zip|tgz|tar\.gz)[^"'\\s)]*/gi) || [];
       for (const u of bare) {
         try {
@@ -511,21 +530,6 @@
           artifactUrls.add(u.replace(/[),.;]+$/g, ""));
         } catch {}
       }
-      // relative paths common in SPAs (e.g. href:"/download/Unfatten16.dmg")
-      const relArtifacts =
-        body.match(
-          /["'`](\/?[^"'`\s)]+\.(?:dmg|pkg|zip|tgz|tar\.gz)(?:\?[^"'`\s)]*)?)["'`]/gi,
-        ) || [];
-      for (const raw of relArtifacts) {
-        const path = raw.replace(/^['"`]|['"`]$/g, "").trim();
-        if (!path || /^https?:\/\//i.test(path)) continue;
-        if (/^(data:|blob:|javascript:)/i.test(path)) continue;
-        try {
-          const abs = new URL(path, pageUrl).href;
-          assertSafePublicFetchUrl(abs);
-          artifactUrls.add(abs);
-        } catch {}
-      }
       // API endpoints that look like version/download manifests
       const apis =
         body.match(
