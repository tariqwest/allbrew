diff --git a/tests/unit/generators/binary-release.test.ts b/tests/unit/generators/binary-release.test.ts
index 1b44375..8db1c93 100644
--- a/tests/unit/generators/binary-release.test.ts
+++ b/tests/unit/generators/binary-release.test.ts
@@ -204,6 +204,15 @@ describe("collectBinaryReleasePayload", () => {
     );
   });
 
+  it("templateReleaseUrl rewrites bare version in asset basename after v-tag path", () => {
+    // Supersonic-style: tag v0.22.0 in path, bare 0.22.0 in zip name
+    const url =
+      "https://github.com/supersonic-app/supersonic/releases/download/v0.22.0/Supersonic-0.22.0-mac-arm64.zip";
+    expect(templateReleaseUrl(url, "0.22.0", "v0.22.0")).toBe(
+      "https://github.com/supersonic-app/supersonic/releases/download/v#{version}/Supersonic-#{version}-mac-arm64.zip",
+    );
+  });
+
   it("detects versioned bare universal binaries (afm_0.1.0_macOS_universal)", async () => {
     const bareRelease = {
       tagName: "0.1.0",
diff --git a/tests/unit/utils.test.ts b/tests/unit/utils.test.ts
index 4f9f82d..81d4da0 100644
--- a/tests/unit/utils.test.ts
+++ b/tests/unit/utils.test.ts
@@ -406,6 +406,15 @@ describe("isAppAsset", () => {
     expect(isAppAsset("tool-macos-x64.zip")).toBe(false);
   });
 
+  it("matches short mac+arch desktop app zips (Electron/Fyne)", () => {
+    // Supersonic releases: Product-version-mac-arm64.zip contains Supersonic.app
+    expect(isAppAsset("Supersonic-0.22.0-mac-arm64.zip")).toBe(true);
+    expect(isAppAsset("Supersonic-0.22.0-mac-x64.zip")).toBe(true);
+    expect(isAppAsset("MyApp-1.0.0-mac-arm64.zip")).toBe(true);
+    expect(matchAssetToArch("Supersonic-0.22.0-mac-arm64.zip")).toBe("macosArm");
+    expect(matchAssetToArch("Supersonic-0.22.0-mac-x64.zip")).toBe("macosIntel");
+  });
+
   it("rejects non-mac .zip files", () => {
     expect(isAppAsset("foo-linux-x64.zip")).toBe(false);
   });
