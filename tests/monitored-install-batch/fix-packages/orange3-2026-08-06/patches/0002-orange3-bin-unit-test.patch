diff --git a/tests/unit/generators/pip-package.test.ts b/tests/unit/generators/pip-package.test.ts
index 83a4ea7..7f9909b 100644
--- a/tests/unit/generators/pip-package.test.ts
+++ b/tests/unit/generators/pip-package.test.ts
@@ -616,6 +616,13 @@ describe("pip requirement parsing helpers", () => {
   it("knows shell-gpt binary alias", () => {
     expect(KNOWN_BIN_NAMES["shell-gpt"]).toBe("sgpt");
   });
+
+  it("knows orange3 console-script alias orange-canvas", () => {
+    expect(KNOWN_BIN_NAMES["orange3"]).toBe("orange-canvas");
+    expect(KNOWN_BIN_NAMES[normalizePackageName("Orange3")]).toBe(
+      "orange-canvas",
+    );
+  });
 });
 
 describe("collectPipPackagePayload — pinned dep version skew", () => {
