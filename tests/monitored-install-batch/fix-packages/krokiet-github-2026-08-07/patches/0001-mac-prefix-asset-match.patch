diff --git a/lib/generators/binary-release.ts b/lib/generators/binary-release.ts
index d6eb00f..2615832 100644
--- a/lib/generators/binary-release.ts
+++ b/lib/generators/binary-release.ts
@@ -44,7 +44,7 @@ export function resolveBinaryReleaseBinName(
     n
       .replace(/\.exe$/i, "")
       .replace(
-        /[-_.]?(darwin|macos|osx|linux|windows|win32|apple)[-_.]?(arm64|aarch64|amd64|x86_64|x64|i386|universal)?$/i,
+        /[-_.]?(darwin|macos|osx|mac|linux|windows|win32|apple)[-_.]?(arm64|aarch64|amd64|x86_64|x64|i386|universal|all)?$/i,
         "",
       )
       .replace(/[-_.]\d+\.\d+(?:\.\d+)*(?:[-_][0-9A-Za-z]+)?$/i, "")
@@ -195,11 +195,32 @@ export async function collectBinaryReleasePayload(
   const license = guessLicenseIdentifier(repoInfo.license);
   const homepage = repoInfo.homepage || repoInfo.htmlUrl;
 
+  /** Prefer formula-name matches and default builds over multi-frontend monorepo noise. */
+  const scoreAssetForFormula = (assetName: string, formulaName: string): number => {
+    const lower = assetName.toLowerCase();
+    const fn = formulaName.toLowerCase().replace(/-/g, "_");
+    const fnDash = formulaName.toLowerCase();
+    let s = 0;
+    if (lower.includes(fn) || lower.includes(fnDash)) s += 100;
+    // Prefer default builds over feature/renderer matrix variants.
+    if (/heif|libraw|avif|skia|femtovg|wgpu|all_backends|gtk/i.test(lower)) s -= 25;
+    if (!lower.includes(".")) s += 5; // bare binary slightly over zip
+    s -= Math.min(lower.length, 80); // shorter names when scores tie-ish
+    return s;
+  };
+
   const archAssets: Record<string, any> = {};
   for (const asset of release.assets) {
     if (!isBinaryAsset(asset.name)) continue;
     const arch = matchAssetToArch(asset.name);
-    if (arch) archAssets[arch] = asset;
+    if (!arch) continue;
+    const prev = archAssets[arch];
+    if (
+      !prev ||
+      scoreAssetForFormula(asset.name, name) > scoreAssetForFormula(prev.name, name)
+    ) {
+      archAssets[arch] = asset;
+    }
   }
 
   if (archAssets.macosUniversal) {
diff --git a/lib/utils.ts b/lib/utils.ts
index 28ec1c1..e19c66d 100644
--- a/lib/utils.ts
+++ b/lib/utils.ts
@@ -541,8 +541,12 @@ export function archPatterns() {
     macosArm: [
       /darwin.*arm64/i,
       /macos.*arm64/i,
+      // Prefix form: mac_krokiet_arm64, mac-tool-arm64 (not "macos"/"darwin")
+      /(?:^|[^a-z])mac[-_.].*arm64/i,
+      /(?:^|[^a-z])mac[-_.].*aarch64/i,
       /arm64.*darwin/i,
       /arm64.*macos/i,
+      /(?:^|[^a-z])arm64[-_].*mac(?:os)?(?:[^a-z]|$)/i,
       /aarch64.*darwin/i,
       /aarch64.*apple/i,
       /apple.*silicon/i,
@@ -554,6 +558,7 @@ export function archPatterns() {
       /macos.*x86_64/i,
       /darwin.*x64/i,
       /macos.*x64/i,
+      /(?:^|[^a-z])mac[-_.].*(?:amd64|x86_64|x64)(?:[^a-z]|$)/i,
       /x86_64.*darwin/i,
       /x86_64.*macos/i,
     ],
@@ -561,6 +566,14 @@ export function archPatterns() {
       /macos[-_.]?universal/i,
       /universal[-_.]?macos/i,
       /darwin[-_.]?universal/i,
+      /(?:^|[^a-z])mac[-_.]universal/i,
+      // goreleaser fat binaries: wander_1.1.0_Darwin_all.tar.gz
+      /darwin[-_.]all(?:[^a-z]|$)/i,
+      /macos[-_.]all(?:[^a-z]|$)/i,
+      /osx[-_.]all(?:[^a-z]|$)/i,
+      /(?:^|[^a-z])mac[-_.]all(?:[^a-z]|$)/i,
+      /(?:^|[^a-z])all[-_.]darwin(?:[^a-z]|$)/i,
+      /(?:^|[^a-z])all[-_.]macos(?:[^a-z]|$)/i,
     ],
     linuxArm: [
       /linux.*arm64/i,
@@ -664,7 +677,7 @@ export function isBareBinaryAsset(assetName) {
 
   const stripped = lower
     .replace(
-      /[-_.]?(darwin|macos|osx|linux|windows|win32|apple)[-_.]?(arm64|aarch64|amd64|x86_64|x64|i386|universal)?/gi,
+      /[-_.]?(darwin|macos|osx|mac|linux|windows|win32|apple)[-_.]?(arm64|aarch64|amd64|x86_64|x64|i386|universal)?/gi,
       " ",
     )
     .replace(/[-_.]+/g, " ")
diff --git a/tests/unit/utils.test.ts b/tests/unit/utils.test.ts
index 4f9f82d..b05bed7 100644
--- a/tests/unit/utils.test.ts
+++ b/tests/unit/utils.test.ts
@@ -10,6 +10,7 @@ import {
   matchAssetToArch,
   isAppAsset,
   isBinaryAsset,
+  isBareBinaryAsset,
   assertSafeFetchUrl,
   resolveNonCollidingFormulaName,
   resolveNonCollidingCaskName,
@@ -367,6 +368,15 @@ describe("matchAssetToArch", () => {
     expect(matchAssetToArch("foo_aarch64_apple.zip")).toBe("macosArm");
   });
 
+  it("matches mac_ prefix release binaries (czkawka/krokiet style)", () => {
+    expect(matchAssetToArch("mac_krokiet_arm64")).toBe("macosArm");
+    expect(matchAssetToArch("mac_krokiet_heif_avif_arm64")).toBe("macosArm");
+    expect(matchAssetToArch("mac_czkawka_cli_arm64")).toBe("macosArm");
+    expect(matchAssetToArch("mac_krokiet_all_backends_arm64.zip")).toBe("macosArm");
+    expect(isBinaryAsset("mac_krokiet_arm64")).toBe(true);
+    expect(isBareBinaryAsset("mac_krokiet_arm64")).toBe(true);
+  });
+
   it("matches macOS Intel patterns", () => {
     expect(matchAssetToArch("foo-darwin-amd64.tar.gz")).toBe("macosIntel");
     expect(matchAssetToArch("foo-macos-x86_64.tgz")).toBe("macosIntel");
