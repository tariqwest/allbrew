diff --git a/lib/generators/binary-release.ts b/lib/generators/binary-release.ts
index d6eb00f..f898beb 100644
--- a/lib/generators/binary-release.ts
+++ b/lib/generators/binary-release.ts
@@ -290,7 +290,9 @@ export async function collectBinaryReleasePayload(
     livecheckBlock: githubLatestLivecheckBlock(repoInfo.fullName, ":stable"),
     allbrewDependency: rubyEscape(getAllbrewFormulaDependency()),
     testBinName: rubyEscape(binName),
-    serviceBlock: buildServiceBlock(serviceFromOptions(options, name), name),
+    // Use the installed binary name (not the formula name) so core-collision
+    // renames like godns → godns-tap still produce `run opt_bin/"godns"`.
+    serviceBlock: buildServiceBlock(serviceFromOptions(options, binName), binName),
   };
 }
 
@@ -316,7 +318,11 @@ export function templateReleaseUrl(
       replacement = "#{version}";
     }
     out = out.split(tag).join(replacement);
-  } else if (ver && out.includes(ver)) {
+  }
+
+  // Also rewrite remaining bare version segments (asset basenames often omit the
+  // leading "v": .../v#{version}/Supersonic-0.22.0-mac-arm64.zip).
+  if (ver && out.includes(ver)) {
     out = out.split(ver).join("#{version}");
   }
 
