diff --git a/lib/classifier.ts b/tmp/allbrew-wt-aldente-pro-12039/lib/classifier.ts
index 97cba82..2feea7c 100644
--- a/lib/classifier.ts
+++ b/tmp/allbrew-wt-aldente-pro-12039/lib/classifier.ts
@@ -105,7 +105,7 @@ export function classify(url) {
     return { type: 'bash-script', url };
   }
 
-  if (path.endsWith('.dmg')) {
+  if (path.endsWith('.dmg') || path.endsWith('.pkg')) {
     return { type: 'cask-dmg', url };
   }
 
@@ -132,8 +132,12 @@ export async function classifyWithHead(url) {
 
     const ct = (response.headers.get('content-type') || '').toLowerCase();
     const disp = (response.headers.get('content-disposition') || '').toLowerCase();
+    let headPath = '';
+    try { headPath = new URL(response.url || url).pathname.toLowerCase(); } catch { /* ignore */ }
 
-    if (ct.includes('application/x-apple-diskimage') || disp.includes('.dmg')) {
+    if (ct.includes('application/x-apple-diskimage') || disp.includes('.dmg') ||
+        ct.includes('application/vnd.apple.installer') || disp.includes('.pkg') ||
+        headPath.endsWith('.pkg')) {
       return { type: 'cask-dmg', url };
     }
 
