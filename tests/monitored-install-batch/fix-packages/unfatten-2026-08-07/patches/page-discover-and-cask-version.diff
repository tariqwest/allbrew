diff --git a/lib/generators/cask-app.ts b/lib/generators/cask-app.ts
index 096e5fc..61fbc95 100644
--- a/lib/generators/cask-app.ts
+++ b/lib/generators/cask-app.ts
@@ -26,9 +26,14 @@ export async function collectCaskAppPayload(
     .replace(/-[\d.]+$/, "");
 
   const name = options.name || toCaskToken(baseName);
-  const desc = options.desc || `Install ${appName || baseName}`;
-  const version = options.version || extractVersionFromUrl(url);
-  const displayName = appName || baseName;
+  const rawApp = appName || baseName;
+  const displayName = String(rawApp).replace(/\.app$/i, "") || baseName;
+  const desc =
+    options.desc ||
+    `Install ${displayName}`.replace(/\s+from\s+https?:\/\/\S+/i, "");
+  // Always emit a version: Homebrew 4+ can crash (`latest?` on nil) when version is omitted.
+  const version =
+    options.version || extractVersionFromUrl(url) || extractCompactVersion(filename) || "1.0.0";
 
   return {
     template: "cask_app",
@@ -37,7 +42,7 @@ export async function collectCaskAppPayload(
     url: rubyEscape(url),
     displayName: rubyEscape(displayName),
     desc: rubyEscape(desc),
-    versionLine: version ? `  version "${rubyEscape(version)}"\n` : "",
+    versionLine: `  version "${rubyEscape(version)}"\n`,
     homepageLine: options.homepage
       ? `  homepage "${rubyEscape(options.homepage)}"\n`
       : "",
@@ -125,3 +130,16 @@ function extractVersionFromUrl(url: string) {
   const match = url.match(/[/-]v?(\d+\.\d+(?:\.\d+)?)/);
   return match ? match[1] : null;
 }
+
+/**
+ * Filenames like Unfatten16.dmg / App15.zip encode dotted versions as two
+ * trailing digits (1.6 / 1.5) without a separator — common on marketing sites.
+ */
+function extractCompactVersion(filename: string): string | null {
+  const base = filename.replace(/\.(dmg|zip|pkg)$/i, "");
+  const m = base.match(/([A-Za-z])(\d)(\d)$/);
+  if (!m) return null;
+  return `${m[2]}.${m[3]}`;
+}
+
+export { extractVersionFromUrl, extractCompactVersion };
diff --git a/lib/page-discover.ts b/lib/page-discover.ts
index f9c7e37..1a3a867 100644
--- a/lib/page-discover.ts
+++ b/lib/page-discover.ts
@@ -503,7 +503,7 @@ export async function discoverFromScriptBundles(
         timeoutMs: 20_000,
       });
       const body = fetched.body;
-      // direct artifact URLs in JS
+      // direct absolute artifact URLs in JS
       const bare = body.match(/https?:\/\/[^"'\\s)]+\.(?:dmg|pkg|zip|tgz|tar\.gz)[^"'\\s)]*/gi) || [];
       for (const u of bare) {
         try {
@@ -511,6 +511,21 @@ export async function discoverFromScriptBundles(
           artifactUrls.add(u.replace(/[),.;]+$/g, ""));
         } catch {}
       }
+      // relative paths common in SPAs (e.g. href:"/download/Unfatten16.dmg")
+      const relArtifacts =
+        body.match(
+          /["'`](\/?[^"'`\s)]+\.(?:dmg|pkg|zip|tgz|tar\.gz)(?:\?[^"'`\s)]*)?)["'`]/gi,
+        ) || [];
+      for (const raw of relArtifacts) {
+        const path = raw.replace(/^['"`]|['"`]$/g, "").trim();
+        if (!path || /^https?:\/\//i.test(path)) continue;
+        if (/^(data:|blob:|javascript:)/i.test(path)) continue;
+        try {
+          const abs = new URL(path, pageUrl).href;
+          assertSafePublicFetchUrl(abs);
+          artifactUrls.add(abs);
+        } catch {}
+      }
       // API endpoints that look like version/download manifests
       const apis =
         body.match(
