diff --git a/lib/cli.ts b/lib/cli.ts
index dc1e69f..2a54fdc 100644
--- a/lib/cli.ts
+++ b/lib/cli.ts
@@ -1468,6 +1468,11 @@ async function generateWithConfirmation(generatorName, params: any, opts: any) {
       // class name and bottle block stay aligned with the file name.
       userOpts.name = preferred;
     } else {
+      // Prefer identity-preserving alts (owner-repo / fullName) before bare
+      // short repo names like "cli" which collide-rename poorly (gotify → cli).
+      const owner = params.repoInfo?.fullName?.split?.("/")?.[0];
+      const repoName = params.repoInfo?.name;
+      const fullName = params.repoInfo?.fullName;
       const altSources = [
         params.packageName,
         params.crateName,
@@ -1476,7 +1481,14 @@ async function generateWithConfirmation(generatorName, params: any, opts: any) {
         opts.package,
         opts.crateName,
         opts.binName,
-        params.repoInfo?.name,
+        fullName ? String(fullName).replace(/\//g, "-") : null,
+        owner && repoName ? `${owner}-${repoName}` : null,
+        owner ? `${preferred}-${owner}` : null,
+        // `${preferred}-cli` when the GitHub repo is a *-cli style project
+        repoName && /cli$/i.test(String(repoName))
+          ? `${preferred}-cli`
+          : null,
+        repoName,
       ];
       const resolved = resolveNonCollidingFormulaName(preferred, altSources);
       if (resolved.renamedFrom && resolved.name !== preferred) {
diff --git a/lib/utils.ts b/lib/utils.ts
index 28ec1c1..9518894 100644
--- a/lib/utils.ts
+++ b/lib/utils.ts
@@ -171,6 +171,27 @@ export function isHomebrewCaskToken(name: string): boolean {
  * formula that reuses a core token (e.g. nanobot) silently inherits core
  * metadata — including preserve_rpath? === false — and breaks pip native wheels.
  */
+/** Bare nouns that must not replace a product formula token on collision. */
+const GENERIC_FORMULA_NAME_RE =
+  /^(cli|app|server|client|tool|lib|api|sdk|core|bot|web|ui|bin|cmd|pkg|util|utils|service)$/i;
+
+/**
+ * True when `candidate` is a generic noun that would lose product identity
+ * (e.g. renaming colliding `gotify` → repo name `cli`).
+ */
+export function isGenericFormulaNameAlt(
+  candidate: string,
+  preferred: string,
+): boolean {
+  const c = toFormulaName(candidate || "");
+  const p = toFormulaName(preferred || "");
+  if (!c || !p || c === p) return false;
+  if (!GENERIC_FORMULA_NAME_RE.test(c)) return false;
+  // Keep alts that still carry the preferred product token.
+  if (c.includes(p) || p.includes(c)) return false;
+  return true;
+}
+
 export function resolveNonCollidingFormulaName(
   preferredName: string,
   alternatives: Array<string | null | undefined> = [],
@@ -188,6 +209,9 @@ export function resolveNonCollidingFormulaName(
     const candidate = toFormulaName(String(alt || ""));
     if (!candidate || seen.has(candidate)) continue;
     seen.add(candidate);
+    // Skip bare generic renames (gotify → cli) so we fall through to
+    // identity-preserving candidates or the `${preferred}-tap` fallback.
+    if (isGenericFormulaNameAlt(candidate, preferred)) continue;
     if (!isHomebrewCoreFormulaName(candidate)) {
       return {
         name: candidate,
diff --git a/tests/unit/utils.test.ts b/tests/unit/utils.test.ts
index 4f9f82d..b1e5bd3 100644
--- a/tests/unit/utils.test.ts
+++ b/tests/unit/utils.test.ts
@@ -119,6 +119,26 @@ describe("resolveNonCollidingFormulaName", () => {
     expect(result.name).toBe("nanobot-tap");
     expect(result.renamedFrom).toBe("nanobot");
   });
+
+  it("skips generic bare repo alts like cli when preferred is a product name", () => {
+    mkdirSync(join(coreRoot, "Formula", "g"), { recursive: true });
+    writeFileSync(
+      join(coreRoot, "Formula", "g", "gotify.rb"),
+      "class Gotify < Formula\nend\n",
+    );
+    // Only generic alt → fall through to gotify-tap
+    const onlyGeneric = resolveNonCollidingFormulaName("gotify", ["cli"]);
+    expect(onlyGeneric.name).toBe("gotify-tap");
+    expect(onlyGeneric.renamedFrom).toBe("gotify");
+
+    // Identity-preserving alt wins over generic
+    const withOwnerRepo = resolveNonCollidingFormulaName("gotify", [
+      "cli",
+      "gotify-cli",
+    ]);
+    expect(withOwnerRepo.name).toBe("gotify-cli");
+    expect(withOwnerRepo.renamedFrom).toBe("gotify");
+  });
 });
 
 describe("resolveNonCollidingCaskName", () => {
